// Route wrapper around CalendarBoard, same pattern as KanbanPage and ChatPage.

import CalendarBoard from '../components/calendar/CalendarBoard'

export default function CalendarPage() {
    return <CalendarBoard/>
}